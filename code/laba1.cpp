#include <bits/stdc++.h>
using namespace std;

#define e exp(1)

int main(){
	string in;
	double x;
	cin >> in;
	if (in == "null") //сами инициализиреум x
	else x = stod(in);
	cout << x;
	

	return 0;
}